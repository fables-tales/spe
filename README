this is grapheme, it does some things

SETUP
1) download the google web toolkit and set up an environment variable
   GWT=/path/to/your/gwt/install

2) download the selenium jars from here
  http://code.google.com/p/selenium/downloads/list
  and extract into the depends directory


DEVMODE
*now broken*

TESTING
we have some unit tests which can be run with "ant test.dev"

WEBSOCKETS:
you need to download orbited from here: http://orbited.org/wiki/Download
and point it at the config in config/

to run a simple http server download lighttpd and use the config file in war/

to run everything, start orbited and lighttpd and then point your browser at
localhost:1338/index.html.

in order to rebuild just run at build
